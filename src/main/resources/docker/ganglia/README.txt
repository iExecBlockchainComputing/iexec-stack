Those two docker images are not built for every build. They will be simply built and push to iexec dockerhub repository if necessary. 

Once built and run, the ganglia dashboard can be seen at localhost:80/ganglia.
