
Directory : build/platforms/worker/
Content   : several sub directories
Purpose   : each subdirectory contains "C" libraries for the spcified OS/CPU.
            This is used by "workerallplatforms.sh" script to contruct a single
	    JAR file for all platforms.

See       : build/workerallplatforms.sh

